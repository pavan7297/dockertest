server:
  port: 2321


spring:
  application:
    name: resume

  datasource:
        url: jdbc:postgresql://192.168.1.21:5432/casaos
        username: casaos
        password: casaos
        driver-class-name: org.postgresql.Driver

  jpa:
      hibernate:
          ddl-auto: update
      show-sql: true
      database-platform: org.hibernate.dialect.PostgreSQLDialect

  sql:
      init:
        mode: always